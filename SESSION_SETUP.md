# Session configuration

The application expects these Vercel environment variables:

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
- `SESSION_SECRET`

Keep all three server-side. Never expose them through client-side environment variables or bundle them into frontend code.
